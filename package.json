{
  "name": "csausk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev-legacy": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev . --temp .temp",
    "build-legacy": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build .", 
    "dev": "vuepress dev . --temp .temp",
    "build": "vuepress build ."
  },
  "author": "",
  "license": "GPL",
  "dependencies": {
    "vue-template-compiler": "^2.7.14"
  }
}
