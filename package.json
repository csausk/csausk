{
  "name": "csausk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "vuepress dev . --temp .temp",
    "build": "vuepress build .", 
    "dev-legacy": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress dev . --temp .temp",
    "build-legacy": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build ."
  },
  "author": "kokic",
  "license": "GPL",
  "dependencies": {
    "vue-template-compiler": "^2.7.14"
  }
}
